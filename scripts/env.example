# ════════════════════════════════════════════════════════════════
# 專案敏感變數範本（env protocol）
#
# 使用方式（各客戶專案的設定流程）：
#   1. 本檔由 automated-testing-pipeline 提供。
#   2. 複製到 <your-project>/.testing/.env.example（納入 git）
#   3. 再複製一份為 <your-project>/.testing/.env（.gitignore 排除）
#   4. 編輯 .env 填入實際值
#
# 呼叫流程：
#   run-project.sh 會在啟動測試前自動 `source` 專案的 .testing/.env
#   其中的變數會被注入所有 Docker 容器（compose 服務與 docker run 皆適用）
# ════════════════════════════════════════════════════════════════

# ─── E2E 登入帳密（若 testing.yml 的 tests.e2e.auth.enabled=true 才需填）
E2E_USERNAME=
E2E_PASSWORD=

# ─── k6 壓力測試覆寫（若想覆蓋 testing.yml 預設）
# K6_VUS=10
# K6_DURATION=30s

# ─── 其他敏感資訊（依專案自行擴充，例如）
# API_KEY=
# DB_PASSWORD=
# WEBHOOK_SECRET=

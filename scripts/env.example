# ════════════════════════════════════════════════════════════════
# .env 範本（env protocol）
#
# ★ 本檔完全選填 ★
# 只有在測試需要敏感值（登入帳密、API token）時才建立 .env。
# 一般情況下 4 個 testing.yml 欄位就夠了。
#
# 使用方式：
#   cp scripts/env.example  <project>/.testing/.env.example   # 進 git
#   cp <project>/.testing/.env.example  <project>/.testing/.env  # gitignore
#   vim <project>/.testing/.env
#
# 流水線會自動 source 並注入所有容器。
# ════════════════════════════════════════════════════════════════

# ─── E2E 登入帳密（tests.e2e.auth.enabled=true 時必填）
E2E_USERNAME=
E2E_PASSWORD=

# ─── k6 壓力測試覆寫（可不填，直接用 testing.yml 的值）
# K6_VUS=10
# K6_DURATION=30s

# ─── 專案自己的敏感值（按需新增）
# API_KEY=
# WEBHOOK_SECRET=
#
# 注意：DB 連線資訊「不用」放這裡。
# pipeline 自己管 test-mysql 容器，測試自動拿到 TEST_DB_HOST/PORT/NAME/USER/PASSWORD。
# production DB 永遠不該被自動化測試碰到。
